# Google Agent Development Kit
# extra [db] подтягивает sqlalchemy — нужен для DatabaseSessionService (сессии в Postgres)
google-adk[db]>=0.3.0

# Google GenAI (для types.Content, types.Part и др.)
google-genai>=1.0.0

# Telegram Bot
python-telegram-bot>=20.7

# Environment
python-dotenv>=1.0.0

# Image processing (optional, for future photo support)
Pillow>=10.0.0

# Webhook server for Cloud Run
aiohttp>=3.9.0

# Turso SQLite (Cloud Database) — хранение данных пользователей
libsql-client>=0.3.1

# Async-драйвер Postgres для персистентных ADK-сессий (DatabaseSessionService)
# Используется с SESSION_DB_URL (напр. Neon). ADK требует async-движок.
asyncpg>=0.29.0
